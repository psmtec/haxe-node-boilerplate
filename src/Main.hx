class Main {
	public static function main() {
		trace('haxe-npm-boilerplate');
		trace('--------------------');
		trace('npm run build:dev ... to watch sources and rebuild in debug mode');
		trace('npm run start:dev ... to watch compiled code and restart you app in debug mode');
		trace('');
		trace('npm run build:prod ... to watch sources and rebuild in release mode');
		trace('npm run start:prod ... to watch compiled code and restart you app in release mode');
		trace('');
		trace('npm run lix install gh:USERNAME/REPOSITORY ... install a haxe library from github');
	}
}
